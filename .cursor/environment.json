{
  "name": "Garincha AI Tech",
  "install": "if [ -f package-lock.json ]; then npm ci; elif [ -f package.json ]; then npm install; else echo 'No package.json in this checkout; skipping npm install'; fi"
}
