{
  "name": "Garincha AI Tech",
  "install": "if [ -f package.json ]; then if [ -f package-lock.json ]; then npm ci; else npm install; fi; else echo 'No package.json in this checkout; skipping npm install'; fi"
}
